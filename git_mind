# git config 
git config --global user.name "your name"
git config --global user.email "your email"

#rm 
git rm filename 
git commit -m "remove filename"

#rm from repo, not dir 
git rm --cached filename


